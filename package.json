{
  "name": "lms-project",
  "version": "1.0.0",
  "description": "Learning Management System",
  "main": "index.html",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}